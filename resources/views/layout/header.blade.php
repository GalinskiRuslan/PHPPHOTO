<header>
    <div class="container">
        <a href="{{ route('home') }}">Home</a>
        <a href="{{ route('photos.index') }}">Photos</a>
        <a href="{{ route('photoView') }}">View Photos</a>
    </div>
</header>
